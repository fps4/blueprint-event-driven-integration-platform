# openapi-components

Shared OpenAPI components for connector services (schemas, parameters, headers). Consumers can `$ref` `connectors.yaml` for topic validation, ingest payloads, request-id headers, and standard responses.
